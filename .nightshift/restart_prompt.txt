$nightshift

你在 /home/eric/harness 执行 Nightshift。
当前模式：CONSTRAINED_EXPANSION（约束扩展）。

## 启动要求
- 使用 nightshift skill 的流程执行。
- 基于现有 31 patterns / 12 topics 继续扩展。
- 不要重复已有知识；默认同化，不轻易新建。

## 探索方向
1) Agent SDK 落地实战
   - OpenAI Agents SDK, Claude SDK, Kode SDK, agent sdk go, CrewAI
   - 关注 demo -> production 断裂点：状态管理、多 agent 通信、错误恢复
2) AI 全栈可靠性边界
   - 前端/后端/测试/运维四环节可达上限
   - OpenAI Harness 大规模工程细节 + 社区一线长跑经验
3) PRD -> 产品信息保真
   - PRD -> spec -> arch -> tasks -> code 的信息损失
   - 最佳中间产物格式、是否存在 PRD 直出产品实践
4) AI 代码有效测试
   - AI 典型 bug 模式：表面正确边界错、状态泄漏、隐式依赖
   - property-based / mutation / invariant testing 实战配置
5) 长运行稳定性工程
   - session 长跑退化机理：状态累积、context 退化、compact 降级、token 耗尽
   - 诊断与防御模式

## 重要约束（必须遵守）
- 遵守 `.nightshift/L1-L7-learning-principles.md`。
- per-topic <= 5 patterns。
- active directions <= 15。
- 每 5 cycles 必须压缩（合并优先，禁止纯 split 膨胀）。
- 每轮写完必须执行检索测试，并在 morning-brief 记录结果。

## 每次运行只执行 1 个 cycle（runner 会自动续跑）
1) 从 `.nightshift/state.json` 读取当前 cycle，执行 cycle + 1。
2) 本 cycle 必须真实落盘：
   - `references/patterns/*`
   - `references/patterns/_master_index.md`
   - `morning-brief.md`
   - `.nightshift/state.json`
3) 本 cycle 必须 `git commit`（不要 push）。
4) 禁止修改 `PRD/`、`generator/`、项目源代码。

## 强制信源
- https://t.co/dwAiIjlXet（重定向到 HN Popular Blogs OPML）
- Hacker News top/show/new
- 不确定点补官方文档证据链

## Compact 容错
- 若发生上下文压缩或 token 逼近上限，先完成最小可交付落盘 + commit 再退出。
- 退出前打印：`[COMPACT_SAFE_EXIT] cycle=<N> commit=<SHA> mode=CONSTRAINED_EXPANSION`

完成 1 个 cycle 后立刻退出本次运行。
